# Molecule Studio 수정 요청서 — Codex 전달용

- 작성일·관찰 기준일: 2026-09-14
- 대상: [Molecule Studio](https://molecule-studio-ee6n.vercel.app/)
- 목적: 화학적 오분류를 수정하고, 계산값과 교육용 설명의 의미를 명확히 한다.
- 근거 범위: 공개 웹앱의 화면과 실제 조작 결과. 저장소·백엔드 소스는 검토하지 않았으므로 아래 파일명·함수명·원인은 확정하지 않는다.

## 1. 실제 확인한 사실

| ID | 관찰 내용 | 근거의 성격 |
| --- | --- | --- |
| F01 | `O=C=O` 입력 후 ¹³C NMR에서 `212.5 ppm`, `ketone carbonyl C`가 표시됨 | 재현한 작용기 분류 오류. CO₂는 케톤의 정의에 해당하지 않음. [S1] |
| F02 | 물 예제(`O`)에서 H-bond donor와 acceptor가 모두 `0`으로 표시됨 | 화면에서 확인한 값. 사용 라이브러리의 지표 정의·버전·적용 범위는 미확인. 물은 수소 결합에서 주개·받개 역할을 모두 할 수 있음. [S2] |
| F03 | 물의 모델 좌표에서 H–O–H `104.0°`, O–H `0.97 Å`를 측정함 | 해당 점검 시 계산값. 이를 모든 실행에서 일치해야 할 정답으로 사용하지 않음 |
| F04 | 물과 에탄올의 산소를 선택하면 `정사면체형 · 이상각 about 104.5° for H2O`가 표시됨 | 물의 참고 결합각이 다른 AX2E2 원자에도 함께 표시됨 |
| F05 | 스펙트럼 설명은 작용기·원자 환경별 범위라고 밝히지만 그래프에는 단일 수치의 피크가 표시됨 | 예: 물의 ¹H NMR `3.25 ppm`, 카페인의 UV–Vis `335 nm`. 앱은 실제 공명 신호·계산된 λmax가 아니라고 별도 설명함 |
| F06 | 계산 방법, 스펙트럼 한계, 일부 VSEPR 설명과 입력 오류 안내가 영어로 표시됨 | 화면에서 확인한 언어 구성 |
| F07 | 아세트아마이드 `CC(=O)N`의 질소는 VSEPR을 `unsupported / 판정하지 않음`으로 처리함 | 적용 범위를 벗어난 공명 구조에 대한 판정 보류 확인 |
| F08 | CO₂의 중심 탄소는 `AX2 / 직선형 / 180°`로 표시됨 | VSEPR의 다중 결합 전자영역 처리 확인 |
| F09 | 벤젠 고리를 2D 편집기로 만들면 `C6H6`, `c1ccccc1`로 분석됨 | 구조 편집→분석 연결 확인 |
| F10 | 물의 거리·각도 기록, 노트 저장, 컬렉션 재호출이 작동함. 새로고침 후 컬렉션 항목도 유지됨 | 정상 동작 확인 |
| F11 | `C(C)(C)(C)(C)C` 입력 시 `SMILES could not be parsed or sanitized.` 오류가 표시됨 | 잘못된 원자가 구조에 대한 입력 거부 확인 |

**미검증 범위:** 점검용 브라우저가 WebGL을 지원하지 않아 실제 3D 렌더링·회전 품질을 확인하지 못했다. 모바일 조작, MOL 파일 입출력, 학습 카드의 최종 출력 품질도 미검증이다. 이를 이미 발생한 프로그램 오류로 취급하지 않는다.

## 2. Codex에 전달할 실행 지시

ai가 자의적으로 판단하여 결정한 대답:

이하 우선순위·수정 방식·완료 기준은 위 관찰을 바탕으로 작성한 구현 제안이다. 관찰 사실과 추정 원인을 구분하여 작업한다.

> 이 프로젝트의 실제 코드를 확인하고, 아래 P0·P1 항목을 재현한 뒤 수정해 주세요. 먼저 적용되는 AGENTS.md와 기존 실행·검증 명령을 확인하세요. 이미 해결된 항목은 근거를 남기고 중복 수정하지 마세요. 기존 구조와 저장 데이터의 호환성을 유지하면서 관련 모듈을 수정하세요. 화학 계산 결과를 특정 교과서 숫자에 맞추어 덮어쓰지 마세요. 오류 원인을 확인하고 필요한 회귀 검증까지 완료한 뒤 변경 파일, 수정 근거, 검증 결과와 남은 제한을 보고해 주세요. P2는 선택적 후속 개선안으로 정리해 주세요.

### 작업 우선순위

| 우선순위 | 항목 | 완료 목표 |
| --- | --- | --- |
| P0 | CHEM-01: CO₂의 케톤 오분류 | 실제 결합 환경에 근거해 분류하고 미지원 환경은 판정을 보류 |
| P0 | CHEM-02: 물의 수소 결합 지표 해석 | 계산 정의를 확인하고, 물이 수소 결합 불가능한 것으로 읽히는 표시를 수정 |
| P1 | CHEM-03: 계산값·참고값·이상각 분리 | 수치의 출처와 의미를 명확히 표시 |
| P1 | SPEC-01: 교육용 스펙트럼 범위 표현 | 범위 안내를 정밀한 예측 피크로 오인하지 않도록 변경 |
| P1 | UX-01: 한국어 안내와 오류 피드백 | 학생이 결과의 한계와 수정할 입력을 이해할 수 있게 안내 |
| P2 | 기본·심화 모드, 탐구 노트 구조화, 가독성 개선 | 필수 수정 후 선택적으로 적용 |

## 3. 필수 수정 요구사항

### CHEM-01 — CO₂의 ¹³C NMR 케톤 오분류

**재현:** SMILES에 `O=C=O` 입력 → 분석 → ¹³C NMR 선택 → `212.5 ppm / ketone carbonyl C` 확인.

**수정 요구사항**

- 작용기 분류와 ¹³C NMR 범위 연결 로직을 추적한다. 단순히 `C=O`를 포함한다는 이유로 케톤에 배정하는 기본 분기가 있는지 확인한다. 이것은 소스 확인 전의 원인 가설이다.
- 케톤·알데하이드·카복실산·에스터·아마이드를 실제 이웃 원자, 결합 차수, 필요한 전하·방향족성 조건으로 구분한다. 검증된 기존 분류 기능이 있으면 활용한다.
- CO₂가 케톤 범위로 연결되지 않도록 수정한다. CO₂ 전용 스펙트럼 모델이나 검증된 참고 자료가 없다면 해당 예측을 제공하지 않고 이유를 표시한다.
- 오류를 없애기 위해 임의의 CO₂ 화학적 이동값을 새로 넣지 않는다. 구조별 참고값을 추가하는 경우에는 출처와 조건을 함께 저장한다.
- 같은 작용기 분류를 공유하는 IR·UV–Vis 등에서도 잘못된 결과가 파생되는지 확인한다. 확인한 결함만 수정하고, 미지원 구조를 임의의 유기 작용기 범위에 넣지 않는다.

**완료 기준:** CO₂에 케톤 분류 및 그에 종속된 `212.5 ppm`이 나타나지 않는다. 실제 케톤인 아세톤의 분류는 유지된다. 에스터·아마이드 등의 비케톤 카보닐도 케톤으로 잘못 분류되지 않는다. [S1]

### CHEM-02 — 물의 수소 결합 지표와 계산 정의

**재현:** 물 예제 선택 → 분자 정보에서 donor `0`, acceptor `0` 확인.

**수정 요구사항**

- HBD/HBA를 계산하는 라이브러리·함수·버전과 입력 구조의 수소 처리 방식을 확인한다. 원자가 모델 오류인지, 특정 분자 서술자의 정의·적용 범위 때문인지 먼저 구분한다.
- `수소 결합 주개·받개 역할`, `분자 서술자에서 세는 원자/부위 수`, `동시에 형성할 수 있는 수소 결합 수`는 서로 다른 개념임을 UI와 코드의 용어에 반영한다.
- 물에는 주개와 받개 역할을 모두 할 수 있다는 과학적 설명을 제공한다. 수치도 제공한다면 채택한 계산 정의와 맞도록 검증한다. [S2]
- 라이브러리 정의상 물이 제외되는 경우에는 그 숫자를 일반적인 수소 결합 능력처럼 보여주지 않는다. 적용 범위를 설명하거나 해당 교육 화면에서 수치 제공을 보류하는 방식으로 처리한다.
- 화면에서 물만 무조건 `1/1` 또는 `2/2`로 덮어쓰지 않는다. 원시 계산 지표와 별도의 교육용 설명을 제공한다면 두 정보의 의미를 명확히 구분한다.

**완료 기준:** 물이 수소 결합의 주개·받개 역할을 할 수 있다는 사실과 화면 안내가 모순되지 않는다. 숫자의 집계 단위·정의·적용 범위를 확인할 수 있고, 에탄올·에터·아마이드의 판정에도 같은 원칙을 적용한다.

### CHEM-03 — 모델 계산값·실험 참고값·이상각 분리

**재현:** 물의 산소와 에탄올의 산소를 각각 선택 → 물의 `104.5°`가 `이상각`에 포함되어 표시되는지 확인.

**수정 요구사항**

- 다음 정보를 구분하여 표시한다.

| 정보 | 의미 | 표시 예시 |
| --- | --- | --- |
| 모델 좌표에서 계산한 결합각·거리 | 현재 생성한 구조의 좌표로 계산 | `현재 모델의 H–O–H 결합각: …°` |
| 이상적인 전자영역 배치의 각 | 정사면체 등 이상 기하의 각도 | `정사면체 전자영역 배치의 이상각: 약 109.5°` |
| 실험 참고값 | 특정 분자·구조·조건에 대응하는 외부 자료 | `물의 평형 결합각 참고값: 약 104.48° · NIST` |

- 물의 약 `104.5°`를 모든 AX2E2 구조의 보편적인 이상각으로 사용하지 않는다. 에탄올 산소의 국소 구조에는 물의 참고값을 해당 원자의 정답처럼 표시하지 않는다.
- NIST 자료의 물 평형 결합각 `104.4776°`, O–H 길이 약 `0.958 Å`는 참고값으로 취급한다. 현재 모델에서 얻은 `104.0°`, `0.97 Å`와 구분한다. [S3]
- 계산 결과를 실험 참고값에 맞추어 강제로 보정하거나, 물이라는 이유만으로 측정값을 고정하지 않는다. 앱이 안내하는 ETKDG·MMFF 방식의 생성 구조와 실험 구조는 동일하지 않다. [S4, S5]
- 원자를 고르는 순서에서 두 번째 원자가 각의 꼭짓점이라는 점을 안내한다. 단위와 원자 ID를 함께 유지한다.
- 아마이드 등 기존 미지원 VSEPR 환경의 판정 보류를 유지한다. 미지원 구조를 기본 정사면체·삼각뿔형으로 대체하지 않는다.

**완료 기준:** 같은 화면과 학습 카드에서 계산값·참고값·이상각의 의미가 일치한다. 물의 참고값이 에탄올 등의 보편적 결합각처럼 나타나지 않는다. 측정값은 여전히 실제 모델 좌표에서 계산된다.

### SPEC-01 — 교육용 스펙트럼의 범위와 한계 표시

**현재 상태:** 앱은 규칙 기반 범위라고 설명하지만 단일 수치의 피크를 그린다. NMR의 신호 등가성·분할·결합상수·용매·적분을 예측하지 않으며, UV–Vis도 계산된 λmax가 아니라고 명시한다.

**수정 요구사항**

- 현재 엔진의 성격에 맞게 `작용기별 IR 참고 범위`, `원자 환경별 NMR 참고 범위`, `UV–Vis 흡수 영역 개념도` 등으로 표시한다.
- 범위만 제공하는 데이터는 구간 막대·음영 영역 등으로 표현한다. 범위의 중앙값을 실제 피크 위치처럼 크게 강조하지 않는다.
- 시각적 중심점을 사용해야 한다면 `범위 표시를 위한 중심점이며 계산된 피크가 아님`을 명확히 한다. 임의의 높이·폭·면적이 실험 강도나 적분값처럼 해석되지 않도록 한다.
- NMR에서 같은 범위에 속한 원자 수를 실제 신호 수·적분값으로 바꾸어 표시하지 않는다. 이를 계산하지 않는다는 한계를 한국어로 제공한다.
- `자료/규칙 미지원`, `해당 원자 없음`, `예측 범위 미제공`을 구분한다. 예측 자료가 없다는 이유로 실제 흡수가 없다고 단정하지 않는다.
- 화면과 학습 카드에 동일한 값·단위·범위·제한 설명을 사용한다. 현재 문구만 수정하고 내보내기에는 옛 피크 설명이 남는 상태를 방지한다.

**완료 기준:** 학생이 참고 범위를 실험 스펙트럼이나 정밀한 계산 피크로 오인하지 않도록 정보 구조가 정리된다. 현재 규칙에 없는 분자에 임의의 분광학 수치를 생성하지 않는다.

### UX-01 — 한국어 설명과 오류 상태의 일관성

- `H-bond donor/acceptor`는 `수소 결합 주개/받개`와 함께 표시하고, CHEM-02에서 확정한 수치 의미를 짧게 설명한다.
- 계산 구조, 오비탈 개념도, VSEPR 판정 보류, 스펙트럼 한계 등 해석에 중요한 안내는 한국어로 제공한다. SMILES·IR·NMR 같은 표준 약어와 단위는 유지한다.
- 입력 오류에는 사용자가 수정할 대상을 안내한다. 세부 원인을 엔진이 제공하면 괄호·고리 표기·허용 결합 수 등으로 구분한다. 원인을 특정할 수 없으면 추측하지 않고 일반적인 입력 확인 안내를 제공한다.
- 잘못된 입력 뒤 이전의 정상 분자 결과를 유지한다면 `입력 분석 실패 — 아래는 이전 분석 결과입니다`처럼 현재 결과의 상태를 설명한다.
- 정상 작동 중인 컬렉션, 측정 기록, 원자 선택, 예제 불러오기 흐름을 유지한다.

**완료 기준:** 주요 과학적 제한과 오류 복구 방법을 한국어로 이해할 수 있다. 분석 실패나 분자 변경 후 다른 분자의 측정값·설명이 현재 분자의 결과처럼 표시되지 않는다.

## 4. 검증 범위와 최소 회귀 사례

기존 테스트·빌드 도구를 우선 사용한다. 화학 분류와 수치 계산에는 의미 있는 회귀 검증을 추가하고, 단순 문구·색상 변경은 화면 확인으로 검증한다. 측정 결과를 특정 소수점 값에 고정하는 테스트 대신 좌표와 계산 결과의 일관성을 확인한다.

### 화학 로직

| 사례 | SMILES | 확인할 결과 |
| --- | --- | --- |
| 이산화탄소 | `O=C=O` | 케톤이 아님. 중심 C의 AX2·직선형 유지. 미지원 분광 자료에 임의값을 배정하지 않음 |
| 아세톤 | `CC(=O)C` | 실제 케톤을 계속 식별함 |
| 아세트알데하이드 | `CC=O` | 알데하이드를 케톤과 구분함 |
| 아세트산 | `CC(=O)O` | 카복실산을 케톤과 구분함 |
| 아세트산 메틸 | `CC(=O)OC` | 에스터를 케톤과 구분함 |
| 아세트아마이드 | `CC(=O)N` | 아마이드를 케톤과 구분함. 질소의 기존 VSEPR 판정 보류 유지 |
| 물 | `O` | 주개·받개 역할 안내가 과학 개념과 일치. AX2E2 유지. 측정값과 실험 참고값 구분 |
| 에탄올 | `CCO` | 산소에 물의 참고각을 보편적 정답처럼 표시하지 않음. 기존 분자 정보·분석 유지 |
| 다이메틸 에터 | `COC` | 수소 결합의 받개 역할과 O–H가 없는 구조를 구분하여 설명 |
| 원자가 오류 입력 | `C(C)(C)(C)(C)C` | 입력 거부와 이해 가능한 오류 안내. 이전 결과의 상태 표시 |

### 정상 기능 유지와 미검증 기능의 확인

- 기존 물·에탄올·카페인·아스피린 예제 분석과 2D 벤젠 고리 생성이 유지되는지 확인한다.
- 물의 거리·각도 측정 → 기록 → 노트 저장 → 컬렉션 재호출·새로고침 흐름에서 데이터가 일치하는지 확인한다. 분자 변경 시 원자 ID·선택 상태·측정값이 잘못 연결되지 않도록 한다.
- WebGL 사용 가능한 실제 브라우저에서 3D 표시, 회전·확대, 원자 선택, H·라벨 표시를 확인한다. 점검 환경의 WebGL 제한은 앱 자체 결함으로 보고하지 않는다.
- 오비탈이 개념도라는 안내와 지원 범위가 실제 화면에 나타나는지 확인한다. 렌더링 성공을 양자화학적 정확성 검증으로 표현하지 않는다.
- 준비된 비민감 MOL 예제와 학습 카드 출력을 점검한다. 카드에 수정된 범위·단위·한계 설명이 반영되는지 확인한다.
- 모바일은 기존 반응형 화면에서 주요 버튼 접근·원자 선택·스크롤이 가능한지 확인한다. 실제 문제가 재현되면 해당 부분을 보완한다.
- 환경 때문에 수행하지 못한 항목은 `미검증`으로 남기고, 필요한 환경과 확인할 동작을 기록한다. 필수 화학 로직 검증을 마친 뒤 관련 없는 테스트 확장은 하지 않는다.

## 5. P2 — 선택적 후속 개선

아래 항목은 확인된 버그가 아닌 수업 활용을 위한 설계 제안이다. P0·P1 수정과 구분해 적용 여부를 정리한다.

| 제안 | 구체적인 구성 |
| --- | --- |
| 기본·심화 모드 | 기본은 구조·VSEPR·측정·노트 중심. 심화에서 스펙트럼·오비탈·계산 방법을 펼쳐 볼 수 있게 구성 |
| 탐구 노트 구조화 | `예측 → 관찰 → 차이 설명 → 설명 수정` 입력을 선택적으로 제공. 자유 노트도 유지 |
| 가독성 개선 | 실제 화면에서 작은 설명문·대비·터치 영역을 확인하여 필요한 부분 조정 |
| 비교 학습 | 검증된 분자끼리 구조·각도·설명을 비교하는 기능을 후속 과제로 검토 |

## 6. 작업 완료 보고 형식

1. **항목별 상태:** CHEM-01·02·03, SPEC-01, UX-01을 `수정 완료 / 이미 해결됨 / 부분 완료 / 미검증`으로 구분한다.
2. **변경 내용:** 실제 변경한 파일·함수와 원인, 수정 후 동작을 설명한다. 소스 확인 전 가설을 확정 사실처럼 반복하지 않는다.
3. **검증 결과:** 실행한 명령과 주요 입력→결과를 기록한다. CO₂ 오분류 해소, 물 지표의 정의, 모델 좌표 기반 측정 유지 여부를 포함한다.
4. **남은 제한:** 미지원 화학 환경, 미검증 기기·입출력, P2 후속 개선을 분리한다.

>‐---------------------------<

## 7. 근거 자료

- **[S1]** [IUPAC Gold Book — ketones](https://goldbook.iupac.org/terms/view/K03386/pdf): 케톤의 구조 정의. CO₂ 오분류를 판정한 근거.
- **[S2]** [OpenStax Chemistry 2e — Intermolecular Forces](https://openstax.org/books/chemistry-2e/pages/10-1-intermolecular-forces): 물 분자 간 수소 결합 설명. 특정 라이브러리의 HBD/HBA 수치 정의를 입증하는 자료는 아님.
- **[S3]** [NIST CCCBDB — Experimental data for H₂O](https://cccbdb.nist.gov/exp2x.asp?casno=7732185): 평형 결합각 104.4776°, O–H 평형 길이 약 0.958 Å. 모든 생성 구조를 이 값으로 강제하는 근거가 아님.
- **[S4]** [RDKit Book — Conformer Generation](https://www.rdkit.org/docs/RDKit_Book.html#conformer-generation): 구조 생성 방법과 매개변수 설명. 실제 프로젝트의 구현·설정 일치 여부는 소스에서 확인할 것.
- **[S5]** [RDKit — rdForceFieldHelpers](https://www.rdkit.org/docs/source/rdkit.Chem.rdForceFieldHelpers.html): MMFF 등 힘장 최적화 API 설명.

이 문서의 출발점은 2026-09-14에 확인한 공개 배포본이다. 이후 코드가 변경되었을 수 있으므로 실제 작업 시 항목별 재현 결과를 기준으로 수정한다.
