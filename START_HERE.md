# 아스트라에서 시작하기

1. 이 폴더를 새 작업폴더에 풉니다. 오르카·Codex의 모델 선택에서 Astra를 선택합니다.
2. 작업폴더를 열고 아래 문장을 붙인 뒤 만들고 싶은 결과를 설명합니다.
3. AI가 목적과 완료 기준을 작성하고 작업·검증·인계까지 진행합니다.

```text
이 폴더의 UAISE Astra 구조를 사용해 아래 작업을 완성해줘.
내 요청을 INTENT.md와 project/contract.json에 반영하고, 필요한 자료만 읽어.
요청 범위의 로컬 제작·실행·확인·오류 수정은 이어서 진행해.
사용 가능한 완성 결과와 실제 확인한 증거를 전달해줘.
작업: [원하는 결과, 사용 환경, 꼭 필요한 조건]
```

사용자가 명령을 외울 필요는 없습니다. AI가 사용하는 최소 흐름은 다음과 같습니다.

```bash
python tools/uaise.py start
# 실제 작업과 필요한 검토
python tools/uaise.py finish
```

start는 목적·완료 조건의 형식을 확인합니다. 구현 전에는 산출물 파일이나 검사 성공 기록이 없어도 됩니다. finish는 필요한 검사를 수행하고, 이미 통과한 최신 검증 기록이 있으면 재사용합니다. 파일·완료 조건·검토 증거가 바뀌면 재검증합니다.

상태는 `python tools/uaise.py status`, 명시적 검증은 `python tools/uaise.py verify`로 확인합니다. 배포·게시가 포함되더라도 이 로컬 도구 자체가 외부 시스템으로 배포하지는 않습니다.

Python 3.10 이상이 필요합니다. 이 패키지는 모델을 설치하거나 오르카의 모델 설정을 변경하지 않습니다. 실행 환경을 먼저 확인하려면 `python tools/uaise.py doctor`를 사용합니다.
